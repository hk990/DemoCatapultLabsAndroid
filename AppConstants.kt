package io.connect.app.utils

object AppConstants {

    // Login
    const val EMAIL = "email"
    const val PASSWORD = "password"
    const val OTP = "otp"
    const val FA_REQUIRED = "2FA_REQUIRED"

    // VPN Credentials
    const val VPN_CONNECT = "VPN"
    const val VPN_USER_NAME = "vpn"
    const val VPN_PASSWORD = "vpn"

    // VPN Event
    const val CONNECT = "connect"
    const val CONNECTED = "connected"
    const val CONNECTING = "connecting"
    const val CONNECTION_STATE = "connectionState"
    const val STATE = "state"
    const val DURATION = "duration"
    const val BACKGROUND_CONNECTION = "background"

    // VPN Connection Status
    const val VPN_DISCONNECTED = "DISCONNECTED"
    const val VPN_CONNECTED= "CONNECTED"
    const val VPN_RECONNECTING = "RECONNECTING"
    const val VPN_NO_NETWORK = "NONETWORK"

    const val KEY_DATA = "keyData"
    const val KEY_TITLE = "title"
    const val PRIVACY_POLICY = "privacy"
    const val TERMS_CONDITIONS = "terms"
    const val TERMS_URL = "https://connect.io/terms-of-service"
    const val PRIVACY_URL = "https://connect.io/privacy-policy"
    const val WEB = "web"

}